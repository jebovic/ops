---

- name: install apt packages
  apt:
    name:  "{{ item }}"
    state: present
    install_recommends: false
    update_cache:       true
  with_items:
    - curl
    - wget
    - htop

- name: install pip packages
  pip:
    name: httpie